apiVersion: apps/v1
kind: Deployment
metadata:
  name: acmeair-sample-0
  labels:
    app: acmeair-app-0
spec:
  replicas: 1
  selector:
   matchLabels:
     app: acmeair-deployment
  template:
    metadata:
      labels:
        name: acmeair-deployment
        app: acmeair-deployment
        app.kubernetes.io/name: "myapp"
        version: v1
    spec:
      volumes:
      - name: test-volume
        hostPath:
          path: "/root/icp/jLogs"
          type: ""
      containers:
      - name: acmeair-libertyapp
        image: dinogun/acmeair-monolithic
        imagePullPolicy: IfNotPresent
        env:
        - name: "MONGO_HOST"
          value: "acmeair-db-0"
        ports:
          - containerPort: 8080
        resources:
         requests:
          cpu: 2
          memory: 512M
         limits:
          cpu: 4
          memory: 1024M
        volumeMounts:
          - name: "test-volume"
            mountPath: "/opt/jLogs"
---
apiVersion: v1
kind: Service
metadata:
  name: acmeair-service-0
  labels:
    app: acmeair-app-0
spec:
  type: NodePort
  ports:
    - port: 8080
      nodePort: 32221
      targetPort: 8080
      name: http
  selector:
    name: acmeair-deployment
